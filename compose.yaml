services:
  trading-bot:
    build: .
    image: "docker.io/sofie/trading-bot:latest"
    platform: linux/amd64
    volumes:
      - bot-data:/app/data
      - /run/rofl-appd.sock:/run/rofl-appd.sock
    environment:
      - RPC_URL=https://sepolia.base.org
volumes:
  bot-data:
